import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const AppContextProvider = () => {
  return (
    <View>
      <Text>AppContextProvider</Text>
    </View>
  );
};

export default AppContextProvider;

const styles = StyleSheet.create({});
